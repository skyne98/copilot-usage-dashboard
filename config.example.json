{
  "server": {
    "host": "127.0.0.1",
    "port": 3000
  },
  "github": {
    "//token": "PAT with Copilot metrics access. Enterprise: 'manage_billing:copilot' OR 'read:enterprise' (fine-grained: View Enterprise Copilot Metrics). Org: 'read:org'. Leave empty to run on demo data.",
    "token": "",
    "apiBaseUrl": "https://api.github.com",
    "apiVersion": "2022-11-28"
  },
  "targets": {
    "//": "Set 'enterprise' and/or 'organizations'. Copilot metrics need >=5 active members or the API returns empty.",
    "enterprise": "",
    "organizations": [],
    "teams": []
  },
  "defaults": {
    "scope": "enterprise",
    "days": 28
  },
  "limits": {
    "//": "Optional admin-set per-user AI-credit budgets, keyed by login. GitHub has no public per-user Copilot budget API, so these are configured here. Users over their limit show a red 'over limit +N' tag and sort to the top under 'Sort: Over limit'. Omit a login (or set limits to {}) to show 'no limit'.",
    "alice": 500,
    "bob": 1000
  },
  "useSampleDataFallback": true
}
